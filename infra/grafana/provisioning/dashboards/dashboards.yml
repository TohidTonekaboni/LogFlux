apiVersion: 1

providers:
  - name: LogFlux
    orgId: 1
    folder: LogFlux
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
